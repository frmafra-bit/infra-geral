---
name: integracao-qive
description: Nova frente do projeto Davi — integração com a plataforma fiscal Qive (NFe/NFSe/CTe → Contas a Pagar SAP)
metadata: 
  node_type: memory
  type: project
  originSessionId: 3c164253-460f-45fc-b134-e695e45e6667
---

Frente adicionada em 22/06/2026 ao [[proximos-portais-davi]]: integração com a **Qive** (ex-Arquivei), plataforma que captura automaticamente da Sefaz os documentos fiscais (NFe, NFSe, CTe, MDFe) e automatiza Contas a Pagar. Encaixe na arquitetura: Qive → API → SAP B1 (A/P Invoices via Service Layer) → frente Financeira (Bruna). Concilia notas por CNPJ — a base de fornecedores já padronizada neste projeto é pré-requisito e está pronta.

**API VALIDADA EM 01/07/2026** (o CEO enviou as credenciais): base **`https://api.arquivei.com.br/v1`** (a `api.qive.com.br` NÃO responde — usar arquivei.com.br). Auth por headers **`X-API-ID`** e **`X-API-KEY`**. Endpoints: `/nfe/received` ✅ e `/nfse/received` ✅ (200); `/cte/received` ❌ 404 (provável não habilitado no plano). Resposta: `data[]` = {`access_key`, `xml` (base64)}, paginação por **cursor** (`page.next`), `count`, `signature`. O **destinatário (dest CNPJ)** do XML identifica a empresa do Davi → rotear pra base/CompanyDB certa; **emit CNPJ** = fornecedor. Ex. validado: NF do fornecedor TAGA p/ DAVI ALVES DE OLIVEIRA (00700428000147). **Credenciais NÃO ficam em memória — guardar em `.env` no servidor** (a pedido do CEO em 01/07 foram passadas no chat, mas persistir só em env var). Pendências antigas de chave expirada: RESOLVIDAS.

Outras pendências: (1) ~~chave~~ ✅ obtida. (2) enviar à Qive a relação de cidades p/ NFSe — já gerei `Qive_cidades_NFSe.csv` (134 cidades a partir das cidades dos fornecedores; revisar duplicidade de acento/"SP/SP"). (3) credenciais de prefeituras; (4) CFe SAT pendente no time da Qive.

Contatos: Mayara Dias (mayara.dias@qive.com.br) onboarding Qive; Rafaela Fernandes e Bruna Del Santo (Davi). Doc detalhado: `Integracao_Qive.md` (Google Drive). Próximo técnico: POC de leitura (listar docs Qive) + gravação (criar 1 A/P Invoice no SAP homolog), com casamento por CNPJ.

**CAPTURA DE NFS-e LIGADA (09/07/2026 — Mafra pediu "buscar serviços tb p/ ajudar na entrada das notas"):** as cidades JÁ estão habilitadas no Qive — `/v1/nfse/received` devolve dado (testado). XML é **ABRASF 2.02** (parseável com o `parseXML` genérico já existente). Objeto do Qive p/ NFS-e = `{id, xml}` (SEM chave de 44 díg; uso `doc.id` como chave única). Implementado em `backend/routes/mosaico.js`: `extractNfseData()` (ABRASF → prestador=fornecedor `emit_*`, tomador=nós `dest_*`, `total_vNF/vServico/vISS/aliquota`, retenções PIS/COFINS/CSLL/IR, `itemListaServico`, `discriminacao`, RPS), `sincronizaQiveNfse()` (cursor próprio em `qive_nfse_state.json`, cache em `data/mosaico/nfse/`), endpoint **`GET /api/mosaico/nfse?companyDb=`** (mesma régua de empresa por CNPJ do tomador). Deploy 161 (docker cp + restart) OK; 1ª chamada sincronizou 500 NFS-e reais (marketing, contábil, freios, radiador). Sync incremental de 500 em 500 por chamada (trava 10 págs×50). **UI ENTREGUE (09/07, deploy v2026-07-09T19:25:57Z):** app novo `Apps/NotasServico` (RouteNotasServico/TargetNotasServico no manifest; handler `NotasServico` no Base.controller; MenuItem em Compras "🔴 NOVO — Notas de Serviço (Qive)"; card na Home aba Compras "Notas de Serviço"). Tela = lista de NFS-e (/api/mosaico/nfse) com colunas Emissão/Nº/Fornecedor(prestador)/Empresa(tomador)/Valor/ISS/Item lista + botão detalhe (discriminação+retenções), filtro por fornecedor/CNPJ e por data, rodapé Total serviços + Total ISS. NÃO verificada na tela ao vivo (extensão Chrome caiu) — validar com Ctrl+Shift+R. **FALTA (fase 2b): fluxo p/ Contas a Pagar / NF de serviço** (serviço não tem amarração de item — vai direto p/ lançamento/AP no SAP). Ver [[mosaico-kanban-fiscal]].

**CREDENCIAL MOVIDA PARA .env (10/07/2026 — feito pelo Claude do Mac, auditado e mantido):** `mosaico.js` NÃO tem mais a chave em texto plano. Agora: `const QIVE_ID = process.env.QIVE_API_ID; const QIVE_KEY = process.env.QIVE_API_KEY;` + trava em `qiveGet()` que rejeita com "Credenciais do Qive ausentes: defina QIVE_API_ID e QIVE_API_KEY no .env". Nomes das vars: **QIVE_API_ID / QIVE_API_KEY**. O `.env` existe em `/app/.env` (dentro do container, camada overlay — **NÃO é volume, some se o container for RECRIADO**; sobrevive a restart) e em `~/davi_caminhoes/.env` no host. `server.js` carrega dotenv. **Auditado 10/07: Qive autentica (HTTP 200), sync roda, portal no ar, zero erro.** sha256(16) do mosaico.js correto = `ca069765367d58c2` (Windows sincronizado com o container em 10/07; o antigo com a chave era `d48ff667a54a5d65`).

**⚠️ PENDENTE DE SEGURANÇA:** a chave **NÃO foi rotacionada** — é a MESMA que vazou e continua válida. Ela ainda aparece em `D:\Davi caminhoes\.claude\settings.local.json` (allow-list de um curl) e em transcripts de sessão. O `qive_poc.py` está LIMPO (lê de env var). Histórico do git LIMPO (nunca commitada). **Solução real = pedir chave nova à Qive (Mayara) e colocar só no .env.**

**⚠️ FRAGILIDADE DO DEPLOY:** o backend (incl. TODA a captura de NFS-e) e o `/app/.env` vivem só na camada gravável do container (deploys por `docker cp`). Um `docker-compose up --build`/recriação **reverte o container pra imagem antiga**. Recuperação: o código-fonte completo está no Windows (`D:\portal-davi`), o `.env` no host, e `data/` + `xml/` são bind-mounts (persistentes). Se recriar: `docker cp` do backend+webapp do Windows e do `.env` do host.
