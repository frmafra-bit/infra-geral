---
name: estado-portal-receb-junho2026
description: "Estado do portal em 26/06/2026 — Recebimento/NF prontos, aguardando empacotar p/ Junior"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3c164253-460f-45fc-b134-e695e45e6667
---

Em 26/06/2026 (sexta) foi concluído na branch local `feature/despesas-cotacao` (NÃO publicado): correções de bug no Pedido de Compras (endpoint PurchaseOrders, remoção de U_B1SYS_* no save, array correto DocumentLines no save), e no Recebimento de Mercadoria: despesas vindas do pedido, Total da linha, Desconto %/R$, N° Nota Fiscal + Referência fornecedor, scroll dos itens, ação "Copiar para NF de Entrada" (GRPO→PurchaseInvoice BaseType 20, com CostingCode4 corrigido), e padronização inclusão↔consulta (mesmo formulário; ordem: datas no topo-direita, Chave/Ref/Frete embaixo). Tudo validado (node --check / XML) e confirmado funcionando em aba InPrivate.

**Objetivo do usuário:** atualizar a ferramenta no fim de semana para a equipe testar segunda (29/06/2026). Mas **deploy/publicação depende do Junior** (ver [[equipe-e-governanca-portal]]) — nada sobe sem ele. Próximo passo combinado: empacotar (changelog + commit local) para o Junior publicar.

**Git (26/06 noite):** nosso trabalho commitado LOCAL em `feature/despesas-cotacao` = commit `7a18787` (NÃO enviado). Ricardo subiu `324c496` em `origin/main` (25/06, "core modules + i18n"). Divergiram de `f51ecf5`. **13 arquivos em conflito** (SC, Cotação, Pedido — ambos reescreveram pesado, abordagens diferentes); resto (Recebimento/NF/Mapa/css) só nosso, sobe limpo. Ricardo tem i18n; nós temos fixes que ele não tem (ex.: remoção U_B1SYS_*, value-help _setVHLine). **Decisão pendente do Mafra** (manter dele/nosso/merge cirúrgico). Análise completa em `D:\Davi caminhoes\Analise_Nosso_vs_Ricardo.md`. Nada mesclado/enviado — reversível.

**Por que o cliente reclamou da versão do Ricardo (analisado 26/06, commit 324c496):** (1) impostos com `_expenseCodes` fixos ERRADOS em Cotação e Pedido: `{Frete:1, IPI:2, ICMSST:3, Outros:5}` — mas no SAP 2=Seguro, 3=Outros, 5=não existe → IPI grava como Seguro, ICMS-ST como Outros, e "Outros" (cód 5) faz o SAP REJEITAR o documento (erro ao salvar). (2) desconto só em %, sem o R$ que o cliente pediu. Correção: ler códigos via GET /AdditionalExpenses (não fixar) + cadastrar IPI/ICMS-ST no SAP + add desconto R$. Detalhe em `D:\Davi caminhoes\Analise_Problemas_Ricardo.md`.

**Infra ambiente de teste (montado 26/06):** VPS teste `161.97.185.151` (Ubuntu 22.04, user `administrador`, tem Docker e sudo c/ senha). SAP da Ativy tem whitelist por IP — esse VPS NÃO é liberado. Solução: **túnel** via servidor liberado/produção `213.199.51.121` (root, roda o portal prod na 3003, IP liberado na Ativy, SAP em `b1.ativy.com`=`177.85.33.53:17162`). No VPS teste: DNS corrigido (8.8.8.8), `/etc/hosts` `127.0.0.1 b1.ativy.com`, serviço systemd **`tunel-sap`** (autossh) persistente encaminha 17162→liberado→SAP. **Provado: Login Service Layer HTTP 200 pela rota.** Minha chave SSH (`~/.ssh/davi_vps_teste`) instalada nos dois; chave de túnel do VPS teste autorizada no liberado. Falta: subir o portal corrigido no VPS teste (Docker) p/ Rute validar.

**Portal de teste NO AR (26/06):** rodando no VPS teste em **http://161.97.185.151:8080/index.html** — acessível externamente, login SAP HTTP 200 pela rota. Stack: container Docker `davi_caminhoes` (porta 3003, código copiado de produção `/var/www/davi_caminhoes` para `/home/administrador/davi_caminhoes`, git origin REMOVIDO p/ isolar do repo do Ricardo) + **nginx** na 8080 (`/b1s`→SAP via rota, `/`→container) + serviço `tunel-sap`. Docker daemon habilitado. ufw libera 8080. Hoje roda a versão do Ricardo (324c496, com bugs) — próximo: aplicar nossas correções nessa cópia e `docker compose up -d --build` p/ Rute revalidar.

**Correções aplicadas no VPS teste (26/06):** nos 4 controllers de Compras (PedidoCompras Incluir/DetalhePC, CotacaoCompras Add/Detalhe) o `_expenseCodes` foi trocado de `{Frete:1,IPI:2,ICMSST:3,Outros:5}` (código 5 inexistente = crash) para `{Frete:1,IPI:2,ICMSST:4,Outros:3}` (todos válidos no SAP, sem duplicar) → despesas param de travar. Backups `.bak_ricardo` ao lado de cada arquivo (rollback). Editado LOCAL (/tmp/fix → D:\Davi caminhoes\_fix), validado node --check, enviado por SFTP, `docker compose up -d --build`. CAVEAT: IPI grava sob "Seguro" e ICMS-ST sob "Desp.Importação" (labels provisórios até criar códigos próprios no SAP = decisão "fazer correto"). Total divergente: CORRIGIDO (26/06) — os 6 pontos do padrão `if(lineTotalVal===0){recalc}else{mantém}` nos 4 arquivos trocados por recálculo sempre `preço×qtd×(1-desc%)` (deixei intacto só o bloco de save numérico do IncluirPC). Validado node --check, enviado e rebuild. Ambos os erros do Ricardo (despesas + total) agora resolvidos no VPS teste; produção e repo do Ricardo intocados. Decisão "fazer correto" (merge final / criar IPI-ICMS-ST no SAP / i18n) ainda pendente do Mafra.

**Fix edição da Cotação (26/06):** na versão do Ricardo o CotacaoDetalhe nascia com `Editable:false` e NUNCA virava true → não dava pra editar/gravar. Corrigido em preencheModeloCotacao: `oData.Editable = (data.DocumentStatus === "bost_Open")`. Cotação 22 = DocEntry 63, bost_Open. Deployado no VPS teste. ATENÇÃO: verificar se o Pedido (DetalhePC do Ricardo) tem o MESMO problema de Editable — provavelmente sim.

**VPS teste agora roda A NOSSA versão (26/06):** decisão do Mafra — trazer nossas versões que já funcionam. Empacotei nossa `webapp` inteira (branch feature/despesas-cotacao, 3.4MB, exclui test/) → substituí no VPS teste (backup em `webapp.bak_ricardo`), mantendo server.js/.env/backend/Login do teste. Rebuild OK, portal+login 200. Agora o teste tem TODAS nossas melhorias: desconto %/R$ em Cotação/Pedido/Receb/NF, Projeto+4 dimensões em todos, Recebimento completo, Copiar p/ NF, padronização. NÃO tem os impostos IPI/ICMS-ST do Ricardo nem o i18n dele (parte da "decisão correta" do merge final, ainda aberta). Rollback: `webapp.bak_ricardo`.

**Desconto %/R$ uniforme (26/06):** após trazer nossa versão pro VPS teste, faltava desconto na Cotação (nossa não tinha) e na NF. Adicionado Desc.%/R$ na linha em Cotação (Add+Detalhe) e NF Entrada (Adicionar editável + Detalhe read-only). Agora desconto %/R$ sincronizado em TODOS os documentos (Cotação/Pedido/Receb/NF, inclusão+detalhe). Gotcha: na nossa Cotação os campos de linha usam editable="true" fixo (não {/Editable}), então o desconto também precisou de editable="true". Commitado local e600526. Deployado no VPS teste.

**PENDÊNCIA — trocar senha do root:** a senha padrão do Mafra (removida deste arquivo por segurança; é a mesma usada no root do `213.199.51.121` e no sudo do `administrador` no `161.97.185.151`) circulou no chat → trocar. Minha chave SSH já está instalada nos dois servidores, então o acesso continua funcionando após a troca (não preciso da senha, exceto p/ sudo no VPS teste — se trocar, me avise a nova só p/ sudo, ou configure sudo NOPASSWD p/ administrador).

**Lição (cache):** o navegador normal serve a view antiga em cache e mascara mudanças. Sempre validar em janela **InPrivate** ou com DevTools → Rede → "Desabilitar cache". `Ctrl+F5` nem sempre basta para `.view.xml`. Ver [[padrao-formulario-unico-processo]].

**Contas a Pagar — baixa COMPLETA e validada (29/06):** 3 visões (A Pagar/Pago/Baixa) + Excel + ordenar/personalizar nas duas abas + Detalhes do pagamento (títulos baixados + contabilização real com split aplicado×a maior + parceiro de negócio) + filtro Data De/Até + Novo Lançamento Manual (com parcelas editáveis, BPLID 1) + baixa parcial. Bugs resolvidos: conta postável (ActiveAccount+BlockManualPosting), busca conta por trecho/caixa, DocLine p/ baixa de lançamento, fuso na data de reconciliação (usar string YYYY-MM-DD), trava valor pago=aplicado, saldo de NF via PaidToDate distribuído, e **saldo de LANÇAMENTO via "razão por documento"** (a SL não dá BalanceDueCredit do JE → somar SumApplied dos VendorPayments vinculados por JdtNum+Line_ID). Validado: baixa parcial mostra saldo, baixa saldo fecha. PENDENTE CP: desconto/juros na baixa (contas: Desconto Obtido 3.02.01.01.07, Juros Mora 4.02.01.01.09). Limpar pagamentos de teste 13/15 (inconsistentes, pré-trava).

**Desconto/Juros/Abatimento na baixa + CONTAS A RECEBER completo (29/06):** CP baixa agora tem Desconto e Juros por título (colunas no PagamentoDialog) e abatimento de NC/Adiantamento (Sinal -1 = SumApplied negativo). DESCOBERTA SAP (testada via SL): desconto NÃO é aceito em lançamento (`Cash discount is not allowed in this type of document`) e `PaymentAccounts` NÃO pode junto de pagamento a parceiro (`Cannot pay to account when the payment is to a business partner`). SOLUÇÃO SAP-correta e universal: **lançamento de ajuste** contra o parceiro + aplicar as linhas no pagamento. CP: desconto D fornecedor/C Descontos Obtidos (3.02.01.01.07); juros D Juros(4.02.01.01.09)/C fornecedor; aplica desconto SumApplied -tot, juros +tot; TransferSum=ΣSumApplied (trava bate sozinha). Validado SL: VendorPayment DocNum 18,19 (princ 1000, NC -100, desc -30, juros +20 => caixa 890). **Contas a Receber** reconstruído do zero espelhando o CP (a versão do Ricardo era só lista de IncomingPayments): clonei controller+view+fragments de ContasApagar e adaptei p/ cliente — IncomingPayments(rCustomer), Invoices/CreditNotes/DownPayments, JournalEntries cliente no DÉBITO (saldo=Debit-pago), controle Clientes 1.01.03.01.01, desconto concedido 4.01.01.10.01, juros recebidos 3.02.01.01.08, lançamento manual = cliente débito/receita crédito, onPressCliente (fragmento Clientes do Base). Validado SL: IncomingPayment DocNum 1,2 (lançamento manual JdtNum 179 3 parc + baixa parcial c/ desc/juros). DEPLOYADO no VPS teste (docker cp host+container; sem bundle preload; nginx 200). Títulos de teste prontos no portal: **CP JdtNum 188** (FULL HOUSE 3x R$500) e **CR JdtNum 179** (C0001 GABRIELA, parc 2 e 3 abertas R$300). Memória do catálogo: ver [[catalogo-objetos-customizados-sap]]. Próximo: fluxo de caixa; baixa por lote/CNAB; criar IPI/ICMS-ST no SAP; merge p/ Junior.

**Checkpoint git (30/06):** commit LOCAL `39c04c3` em `feature/despesas-cotacao` (D:\portal-davi) — 30 arquivos, todo o bloco Financeiro/Contas a Receber/Estorno-Ver Lançamento/Estoque/regra de custo. NÃO enviado (branch sem upstream; origin = github MafraFiori/DAVI_CAMINHOES do Ricardo, mas nada de push — fica local/nosso). Produção e GitHub do Ricardo intocados. Próximo: rework do FATURAMENTO (telas de venda) — NF de Saída Adicionar está estruturalmente bagunçada (mistura modelo /NFSaida e /Cotacao nos handlers de dimensão CostingCode2-5; changeTotal soma errado/sobrescreve; falta desconto/Item-Serviço/total de linha). Fazer rework limpo espelhando a NF de Entrada, com dados de venda de teste (não existem hoje na base).
